<%= form_for(@zweet) do |f| %>
  <% if @zweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zweet.errors.count, "error") %> prohibited this zweet from being saved:</h2>

      <ul>
      <% @zweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :zombie_id %><br>
    <%= f.text_field :zombie_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
